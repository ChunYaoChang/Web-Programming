const Message = {
  // users(parent, args, { db }, info) {
  //   return db.UserModel.find((user) => {
  //     return user.id === parent.author;
  //   });
  // },
  // comments(parent, args, { db }, info) {
  //   return db.comments.filter((comment) => {
  //     return comment.post === parent.id;
  //   });
  // },
};

export { Message as default };
