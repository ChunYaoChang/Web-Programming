const User = {
  // user(parent, args, { db }, info) {
  //   return db.UserModel.filter((name) => {
  //     return name === name;
  //   });
  // },
  // comments(parent, args, { db }, info) {
  //   return db.comments.filter((comment) => {
  //     return comment.author === parent.id;
  //   });
  // },
};

export { User as default };
